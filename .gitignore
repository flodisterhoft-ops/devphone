node_modules/
dist/
pw-browsers/
selftest*.png
scratch/
scratch/
galaxy.log
st.log
galaxy-test.log
selftest.json
build-info.json

# Local Claude task history (private; backed up separately, never commit)
.claude-sessions/

# debug logs (never commit — may contain local paths)
*.log
